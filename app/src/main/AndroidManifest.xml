<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_apppic"
        android:label="MilkTea-DDHK"
        android:roundIcon="@drawable/ic_apppic"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBanTraSua"
        tools:targetApi="31">
        <activity
            android:name=".GUI.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.ProductDetailsActivity" />
        <activity android:name=".GUI.RegisterActivity"/>
        <activity android:name=".GUI.CartActivity" />
        <activity android:name=".GUI.MainActivity"/>
        <activity android:name=".GUI.AccountActivity"/>
        <activity android:name=".GUI.PaymentActivity"/>
        <activity android:name=".GUI.HistoryActivity"/>
        <activity android:name=".GUI.Home_admin"/>
        <activity android:name=".GUI.ContacActivity"/>

    </application>

</manifest>